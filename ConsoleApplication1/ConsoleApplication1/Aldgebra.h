#include <iostream>
#include <string>
#include <cmath>
using namespace std;
class QuadraticEquation{
public:

	QuadraticEquation(){
		int ax, bx, c, x, discrimenent;
		bool formula;
		formula = ax*x + bx + c;
		cout << "input numbers for a b and c";
		cin >> ax >> bx >> c;
		int b;
		int ac;
		int getDiscrimenent((b*b - 4ac));
		float Root1();
		float Root2();
	}
};